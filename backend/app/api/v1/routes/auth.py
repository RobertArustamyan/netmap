# Routes: POST /auth/invite/{workspace_id}  — generate invite link
#         POST /auth/accept-invite/{token}  — accept invite, join workspace
# Auth (login/signup) is handled entirely by Supabase Auth on the frontend.
