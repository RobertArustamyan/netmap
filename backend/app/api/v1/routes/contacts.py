# Routes: POST   /workspaces/{id}/contacts         — create contact (checks plan limit)
#         GET    /workspaces/{id}/contacts         — list all contacts + edges (graph payload)
#         GET    /workspaces/{id}/contacts/{cid}   — get single contact
#         PATCH  /workspaces/{id}/contacts/{cid}   — update contact
#         DELETE /workspaces/{id}/contacts/{cid}   — delete contact
