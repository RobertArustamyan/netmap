# ORM model: Plan (workspace subscription / tier)
# Fields: id, workspace_id, tier (free | pro | enterprise),
#         stripe_subscription_id, stripe_customer_id,
#         max_members, max_contacts, status (active | past_due | cancelled), updated_at
